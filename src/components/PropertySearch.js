const PropertySearch = () => {
  return (
    <header>
      <h2 className="search__title">Discover Your Dream Home Debrecen</h2>
      <br />
      <input
        type="text"
        className="search__input"
        placeholder="Search by address, neighborhood, city, or ZIP code"
      />
    </header>
  );
};

export default PropertySearch;
